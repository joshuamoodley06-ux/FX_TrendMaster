name: Apply Remaining PR 28 Payload

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    if: github.event.pull_request.head.ref == 'feat/weekly-direction-daily-relationship-v01'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/weekly-direction-daily-relationship-v01
          fetch-depth: 0
      - name: Apply remaining implementation files
        shell: bash
        run: |
          set -euo pipefail
          if [ ! -f .pr28_missing_payload.b64 ]; then
            echo "Remaining payload already applied."
            exit 0
          fi
          base64 -d .pr28_missing_payload.b64 | tar -xz -C python
          rm .pr28_missing_payload.b64
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: complete Weekly direction and Daily trend relationships"
          git push origin HEAD:feat/weekly-direction-daily-relationship-v01
