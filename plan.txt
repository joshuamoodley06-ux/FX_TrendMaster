Let's update the plan:

1. Create `electron/src/viewport/viewportUtils.ts` and `electron/src/viewport/index.ts`.
2. Extract the approved viewport-only functions:
   - structuralRangePaddingCandles (change `StructureLayer` to `string`)
   - structuralRangeFitPadRatio (change `StructureLayer` to `string`)
   - structuralRangeFitDomain (inline a pure string normalization for the layer, or just accept `layer` as string, wait, it takes `range: any`)
   - structuralContextTargetTime
   - clampFitTimesToCandles
   - buildCandleWindowFit
   - candleDataExtent
   - isPlausibleMarketTimeMs
   - parseStructuralTimeMs
   - candleIndexAtOrBefore
   - candleIndexAtOrAfter
   - candleTimeMs
   - normalizeCandleTime
   - StructuralFitWindow (type)

Inside `structuralRangeFitDomain`:
```typescript
  // instead of: const layer = normalizeStructureLayer(range?.structure_layer || range?.layer);
  const rawLayer = String(range?.structure_layer || range?.layer || '').toUpperCase();
  const layerAlias: Record<string, string> = { MN1:'MACRO', W1:'WEEKLY', D1:'DAILY', H4:'INTRADAY', H1:'INTRADAY', M15:'MICRO', M5:'MICRO' };
  const mappedLayer = layerAlias[rawLayer] || rawLayer;
  const layer = ['MACRO', 'WEEKLY', 'DAILY', 'INTRADAY', 'MICRO'].includes(mappedLayer) ? mappedLayer : null;
```
Or I just copy `normalizeStructureLayer` as a local, unexported helper `normalizeLayerString` inside `viewportUtils.ts`. This satisfies the constraints.

I will formulate this in `set_plan` and proceed.
